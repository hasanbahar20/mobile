import * as React from 'react';
import {Icon, MD3Colors} from 'react-native-paper';

const Icons = () => (
  <Icon source="camera" color={MD3Colors.secondary70} size={20} />
);

export default Icons;
