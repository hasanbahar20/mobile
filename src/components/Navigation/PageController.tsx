import React from 'react';
import {PageControl} from 'react-native-ui-lib';

const PageController = () => {
  return <PageControl numOfPages={5} currentPage={0} />;
};

export default PageController;
